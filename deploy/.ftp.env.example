# Copy to deploy/.ftp.env (gitignored). Values from cPanel → FTP Accounts.
# NOT the same as api/db_connect.php (that file is MySQL only).

FTP_HOST=ftp.sfdm.xyz
FTP_USER=linguis1
FTP_PASS=your_cpanel_or_ftp_account_password
FTP_REMOTE_PATH=public_html/api/export_for_supabase.php
